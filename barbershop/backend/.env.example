# Secret NAMES only — do NOT put real values here or commit a filled-in .env.
# Set real values in your deployment's secret store, e.g.:
#   supabase secrets set TWILIO_AUTH_TOKEN=...

# Supabase (Story 1.1 / 1.2)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Twilio (Story 2.x — not needed yet)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
