VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_PLAUSIBLE_DOMAIN=

# Canonical production origin (custom domain), e.g. https://app.truckinglifewithshawn.com
# Set ONLY in Netlify's production context. Leave blank locally and in previews —
# getSiteUrl() falls back to the runtime origin (see src/core/siteUrl.ts).
VITE_SITE_URL=
