// src/modules/settings/Settings.tsx — stub (real screen: Story 1.14 settings
// shell). Eagerly loaded per architecture.md:504 ("Settings eager").
import { Placeholder } from '@/components/Placeholder';

export default function Settings() {
  return <Placeholder title="Settings" />;
}
