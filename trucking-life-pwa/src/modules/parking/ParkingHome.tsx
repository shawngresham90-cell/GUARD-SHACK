// src/modules/parking/ParkingHome.tsx — stub (real screen: Epic 2, Story 2.6).
// Lazy-loaded off the home shell (architecture.md:492, NFR-P6 bundle budget).
import { Placeholder } from '@/components/Placeholder';

export default function ParkingHome() {
  return <Placeholder title="Parking" />;
}
