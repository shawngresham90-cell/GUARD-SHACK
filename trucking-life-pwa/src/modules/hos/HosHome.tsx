// src/modules/hos/HosHome.tsx — stub (real screen: Epic 3). Rendered inside
// <HosShell> by the route tree, which supplies the permanent footer disclaimer.
// Lazy-loaded (architecture.md:493).
import { Placeholder } from '@/components/Placeholder';

export default function HosHome() {
  return <Placeholder title="HOS" />;
}
